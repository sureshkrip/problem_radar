You are categorising a software/automation "problem" onto two independent axes so that the
same recurring **function** across different **industries** can be spotted (spec §4).

You are given the problem's title and statement plus a few representative evidence quotes.

Assign:
- exactly one **primary** industry and one **primary** function,
- optional **secondary** industries/functions when genuinely relevant (keep these short; empty is fine).

Rules:
- You MUST choose only from the provided slug lists. Never invent a slug.
- Use `other` only when nothing else fits; do not reach for it to avoid a decision.
- Judge by the underlying job-to-be-done, not the surface wording.
